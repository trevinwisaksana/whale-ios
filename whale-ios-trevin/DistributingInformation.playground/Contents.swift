//: Playground - noun: a place where people can play

import UIKit
import SwiftyJSON

/*
struct Pager<T> {
    let totalPages: Int = 0
    let pageSize: Int = 3
    let currentPage: Int = 0
    let data: [T]
}

extension Pager {
    init?(json: JSON) {
        guard let pageData: [T] = json.array else {return nil}
        self.data = pageData
    }
}
*/
